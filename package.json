{
  "name": "klara-portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "sass --watch ./sass/main.scss ./css/style.css",
    "live-server": "live-server",
    "start": "npm-run-all --parallel live-server compile"
  },
  "author": "Almost Logical",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
